import type { ExecutiveState } from "../../types/executiveState";import "./DomainViews.css"
export default function RelationshipsView({state}:{state:ExecutiveState}){return <div className="domain-page"><section className="domain-hero"><div className="domain-overline">RELATIONSHIP INTELLIGENCE</div><h2>Relationships</h2><p>Strategic relevance, interaction recency, open loops, and next moves—without becoming a CRM.</p></section><section className="domain-card"><h3>Executive network</h3><div className="domain-sub">Ranked by relevance and open commitments.</div>{state.relationships.length?<div className="domain-list">{state.relationships.map(r=><div className="domain-row" key={r.id}><div className="domain-score">{r.relevance_score??50}</div><div className="domain-row-main"><strong>{r.name}</strong><p>{r.role??""}{r.company?` • ${r.company}`:""} • {r.next_move??r.next_commitment??"No next move set"}</p></div><span className="domain-chip">{r.open_loops??0} loops</span></div>)}</div>:<div className="domain-empty">No relationship records are stored yet. This domain is connected and ready for the first executive relationship dataset.</div>}</section></div>}