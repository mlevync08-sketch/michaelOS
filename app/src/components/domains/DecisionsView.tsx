import type { ExecutiveState } from "../../types/executiveState";import "./DomainViews.css"
export default function DecisionsView({state}:{state:ExecutiveState}){return <div className="domain-page"><section className="domain-hero"><div className="domain-overline">DECISION SUPPORT</div><h2>Decisions</h2><p>Separate choices from tasks. Make tradeoffs explicit and preserve recommendations.</p></section><div className="domain-grid"><section className="domain-card domain-span-8"><h3>Decision queue</h3><div className="domain-sub">{state.decisions.length} open decisions.</div>{state.decisions.length?<div className="domain-list">{state.decisions.map(d=><div className="domain-row" key={d.id}><div className="domain-row-main"><strong>{d.title}</strong><p>{d.recommendation??d.context??"Decision requires executive judgment."}</p></div><span className="domain-chip">{d.confidence??"—"}% confidence</span></div>)}</div>:<div className="domain-empty">No explicit decision records yet. Critical project next-actions are currently supplying decision-like signals.</div>}</section><aside className="domain-card domain-span-4"><h3>Decision guidance</h3><div className="domain-sub">Atlas recommendations.</div>{state.recommendations.length?state.recommendations.map(r=><div className="domain-callout" key={r.id}><small>{r.confidence}% CONFIDENCE</small><p><strong>{r.title}</strong><br/>{r.detail}</p></div>):<div className="domain-callout"><small>RULE</small><p>Reversible decisions should bias toward speed; irreversible decisions should bias toward evidence.</p></div>}</aside></div></div>}