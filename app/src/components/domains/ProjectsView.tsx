import type { ExecutiveState } from "../../types/executiveState";import "./DomainViews.css"
export default function ProjectsView({state}:{state:ExecutiveState}){const red=state.projects.filter(p=>p.health==="red").length;const blocked=state.projects.filter(p=>p.blocker&&p.blocker!=="None").length;return <div className="domain-page"><section className="domain-hero"><div className="domain-overline">EXECUTIVE PORTFOLIO</div><h2>Projects</h2><p>Portfolio health, strategic urgency, blockers, owners, and the next move Atlas believes will create the most leverage.</p></section><div className="domain-stats"><div className="domain-stat"><span>ACTIVE</span><strong>{state.projects.length}</strong></div><div className="domain-stat"><span>CRITICAL</span><strong>{state.metrics.criticalProjects}</strong></div><div className="domain-stat"><span>RED HEALTH</span><strong>{red}</strong></div><div className="domain-stat"><span>BLOCKED</span><strong>{blocked}</strong></div></div><div className="domain-grid"><section className="domain-card domain-span-8"><h3>Attention allocation</h3><div className="domain-sub">Projects ranked by executive urgency.</div><div className="domain-list">{state.projects.map((p,i)=><div className="domain-row" key={p.id}><div className="domain-score">{String(i+1).padStart(2,"0")}</div><div className="domain-row-main"><strong>{p.name}</strong><p>{p.next_action??p.next_milestone??"No next action set"}</p></div><span className={`domain-chip ${p.health==="red"?"red":p.health==="amber"?"amber":""}`}>{p.health}</span><span className="domain-chip gray">{p.priority}</span></div>)}</div></section><aside className="domain-card domain-span-4"><h3>Portfolio guidance</h3><div className="domain-sub">What Atlas sees.</div><div className="domain-callout"><small>TOP MOVE</small><p>{state.mission.title}</p></div><div className="domain-callout"><small>RISK LOAD</small><p>{state.risks.length} material project risks are visible.</p></div></aside></div></div>}